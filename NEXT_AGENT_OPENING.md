你好，你要接手一個進行中的專案，請先讀完這段、照最後的指示做。

## 專案一句話
這是一個「福岡 8/7–8/11 五天四夜」旅遊規劃案。前一階段已完成 5 個行程方案（A–E）的精美簡報，使用者**最終選定「方案 A — 經典均衡」**。**接下來的任務：把方案 A 做成一個互動式網頁地圖。**

## 你的第一步（務必照做）
1. 工作目錄是 `C:\fukuoka`。請先完整讀這份交接文件：**`C:\fukuoka\HANDOFF.md`**（它是自足的，涵蓋行程、已整理好的「可上圖路線」、景點座標表、景點照片、品牌配色、環境與地雷）。
2. 接著讀 **`C:\fukuoka\data\planA.json`**（方案 A 的權威結構化資料）。
3. 瞄一眼 **`C:\fukuoka\assets\img\`** 裡的景點照片（marker 彈窗可用），以及 `assets\credits.json`。

## 重點提醒
- 地圖節點請用 HANDOFF.md **§4「可上圖路線」**與 **§5 座標表**，**不要**直接用 planA.json 的 `spots_all`（那是自動比對、含少數誤判）。
- 方案 A 橫跨三區：福岡市中心、北九州/門司港（Day2）、太宰府（Day3），地圖要能處理大範圍或分日縮放。
- 品牌色：海軍藍 `#1F3A60` × 金 `#C8923A`，底 `#F6F3EC`；字型微軟正黑體（見 §7），地圖建議沿用以保持一致。
- 環境：Windows、Node v22、Python 3.14、LibreOffice 都可用。可用的相關 skills：`mapbox-*` 系列、`frontend-design`、`web-artifacts-builder`（**等使用者選定技術再用**）。

## 然後——待命
讀完後，請用 **3–5 句話**回報你已掌握的內容（方案 A 的五日路線骨架 + 你手上有哪些可用素材），並列出 1–2 個你預期需要使用者拍板的決策點（例如：地圖技術選型 Leaflet/Mapbox/SVG、互動方式、是否分日切換）。

**然後停下來等使用者指示，先不要開始寫任何地圖程式碼。** 使用者會親自說明「要怎麼畫」。在他說明之前，請勿建立檔案或選定技術。
